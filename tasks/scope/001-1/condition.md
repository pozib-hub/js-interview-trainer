# Scope: let и замыкание в while

Что выведется в консоль?

```ts
let i = 10;

while (--i) {
  setTimeout(() => {
    console.log(i);
  });
}
```

Реализуйте функцию `getResult()`, возвращающую массив строк: сначала `"ответ"`, затем описание вывода.

Как исправить — подумайте сами (см. подсказки).
