// TODO: реализуйте — назначьте метод forEach на Array.prototype
// без использования встроенных методов массивов
Array.prototype.forEach = function (
  callback: (value: any, index: number, array: any[]) => void,
  context?: any
): void {
  // TODO: реализуйте
};

export {};
