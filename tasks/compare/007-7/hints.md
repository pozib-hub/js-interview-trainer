## Подсказка 1
Примитивы: `===` (с учётом `NaN` через `Object.is`). Массивы: поэлементно, одинаковой длины.

## Подсказка 2
Объекты: по ключам, рекурсивно. `null` проверяется отдельно (`null === null` → true, `null === undefined` → false). Разные типы → `false`.
