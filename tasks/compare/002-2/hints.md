## Подсказка 1
`||` возвращает первый truthy или последний операнд. `&&` возвращает первый falsy или последний операнд.

## Подсказка 2
`0 || false` → `false` (оба falsy, возвращается последний). `1 && 0 && 5` → `0` (первый falsy). `(1 && 5) || 7` → `5`.
