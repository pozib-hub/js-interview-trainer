## Подсказка 1
Объекты и массивы сравниваются по ссылке, а не по значению. `[] == []` → `false` (разные объекты).

## Подсказка 2
`NaN == NaN` → `false` (NaN не равен самому себе). `null == undefined` → `true` (особое правило).
