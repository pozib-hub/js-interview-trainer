# Compare: undefined и null

Что выведется в консоль?

```ts
console.log(undefined == 0);
console.log(undefined === 0);
console.log(undefined >= 0);
console.log(null >= 0);
console.log(false == 0);
```

Реализуйте функцию `getResult()`, возвращающую массив значений в порядке вывода.
