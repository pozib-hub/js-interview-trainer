## Подсказка 1
`undefined == 0` → `false` (undefined не приводится к числу при `==`). `null >= 0` → `true` (null приводится к 0 при `>=`).

## Подсказка 2
`false == 0` → `true` (false приводится к 0). `undefined === 0` → `false` (строгое равенство без приведения).
