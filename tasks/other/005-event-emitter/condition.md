# Необходимо реализовать класс EventEmitter,

Необходимо реализовать класс EventEmitter, 
  который позволяет подписываться на события с помощью метода subscribe и вызывать их обработчики с помощью метода emit. 
  Класс должен поддерживать возможность подписки нескольких обработчиков на одно событие и корректно вызывать их при генерации события.

  class EventEmitter {
    constructor() {
    }

    public subscribe() {
      
    }

    public emit() {
    }
  }
