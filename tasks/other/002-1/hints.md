## Подсказка 1
`counter` в `one()` — внешняя переменная. `one()` инкрементирует её. В `two()` — локальная `counter = 5`.

## Подсказка 2
`callback()` внутри `two()` — какой `counter` он видит? Внешний или локальный? Зависит от замыкания callback.
