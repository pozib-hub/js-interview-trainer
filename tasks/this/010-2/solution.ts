export function getResult(): string[] {
  return ["ответ", "bar foo", "bar bar", "foo bar", "Объяснение:", "1. Создание объекта Bar. Вызывается конструктор Bar, но перед тем,", "как он выполнится, вызывается super(),", "который запускает конструктор родительского класса Foo.", "2. Выполнение конструктора Foo = super();", "Внутри конструктора Foo выполняется:", "this.id = \"foo\";", "this.print();", "Здесь важно понимать, что this уже относится к Bar, так как объект создаётся именно для Bar", "Какой print() вызовется?", "Метод print() переопределён в Bar, и при вызове this.print() внутри Foo,", "фактически вызывается метод print() из Bar, но значение this.id ещё равно \"foo\"", "3. Продолжаем выполнение конструктора Bar", "После super(); продолжается выполнение кода в Bar:", "this.id = \"bar\";", "this.print();", "Теперь this.id уже \"bar\", и this.print() снова вызывает метод print() из Bar.", "Теперь Bar.print() вызывается, когда this.id = \"bar\"", "4. Вызов super.print();", "super.print();", "Этот вызов явно обращается к print() из Foo, но значение this.id уже \"bar\".", "Вызван Foo.print(), но this.id уже \"bar\""];
}
