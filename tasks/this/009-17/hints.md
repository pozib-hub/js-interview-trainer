## Подсказка 1
`obj.getName()` — вызов как метод, `this = obj`. `unbound.call(obj)` — явная передача `this` через `.call`.

## Подсказка 2
`.bind(obj)()` фиксирует `this` навсегда. `.apply(obj)` — как `.call`, но аргументы массивом. Стрелочная функция не имеет своего `this`.
