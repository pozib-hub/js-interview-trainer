export function getResult(): string[] {
  return [
    "Код export [].sort().filter().reduce().map() не валиден:",
    "1. export без объявления переменной/функции — синтаксическая ошибка",
    "2. [].sort() вызывается на пустом массиве — бессмысленно",
    "3. Цепочка методов .filter().reduce().map() работает, но результат никуда не сохраняется",
    "4. Порядок методов логически неверный: обычно сначала filter, потом map, потом reduce",
  ];
}
