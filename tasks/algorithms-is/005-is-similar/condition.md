# Функция, которая проверяет совпадение массивов

Функция, которая проверяет совпадение массивов

    isSimilar([0,1,2], [2,1,0]) === true
    isSimilar([0,1], [2,1,0]) === false
    isSimilar([0,5,1], [0,1,5]) === true
