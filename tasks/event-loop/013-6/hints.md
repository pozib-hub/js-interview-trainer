## Подсказка 1
Синхронно: `1, "Promise", 4`. Три `setTimeout` в очереди макрозадач. `setTimeout` с `resolve` — второй.

## Подсказка 2
После `resolve` в макрозадаче — микрозадачи `.then` выполняются до следующего `setTimeout`. Цепочка `.then`: `then1`, затем `then2`.
