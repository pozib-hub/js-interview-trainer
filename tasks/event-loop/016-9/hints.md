## Подсказка 1
Синхронно: `1, 2, 8`. `reject()` → `.then` пропускается, `.catch` ловит → `5`. Затем `.then` после `.catch` → `7`.

## Подсказка 2
Второй `.catch` не вызывается (promise уже resolved). `setTimeout(3)` — макрозадача, выполняется после микрозадач.
