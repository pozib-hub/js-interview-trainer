## Подсказка 1
Синхронно: `1, 4, 5`. Затем `a()` рекурсивно вызывает себя → переполнение стека.

## Подсказка 2
`setTimeout` и `Promise.then` не успеют выполниться — стек переполняется раньше. Ошибка: "Maximum call stack size exceeded".
