## Подсказка 1
`async function` — функция, `await` приостанавливает выполнение до resolve. `console.log` в `await` выполняется синхронно.

## Подсказка 2
Микрозадачи (продолжение после `await`, `.then`) выполняются до макрозадач (`setTimeout`).
