## Подсказка 1
Синхронно: `1, 5`. Микрозадача: `4` (внутри — `sleep(1000)` планирует `setTimeout`). `setTimeout(2)` через 1000мс.

## Подсказка 2
`sleep(1000)` разрешится через 1 сек → `3`. `setTimeout(2)` тоже через 1 сек. Порядок зависит от очереди: `2` запланирован раньше `3`.
