# React: typeof JSX

Что вернёт `typeof (<Widget />)` и почему?

```tsx
typeof (<Widget />);
```

Подумайте:
- Во что компилируется JSX?
- Что возвращает `React.createElement`?
- Какой тип у этого значения?
