# Search a 2D Matrix

Search a 2D Matrix

  Вам дана матрица целых чисел m x n со следующими двумя свойствами:
  Каждая строка отсортирована в неубывающем(возрастающем) порядке.
  Первое целое число каждой строки больше последнего целого числа предыдущей строки.
  Если задано целое число, верните true, если цель находится в матрице, или false в противном случае.
  Вам необходимо написать решение со сложностью O(log(m * n)) по времени.

  Input: matrix([[1,3,5,7], [10,11,16,20], [23,30,34,60]], 3) 
  Output: true

  function matrix(array: number[][], target: number) {
  return false;
}
