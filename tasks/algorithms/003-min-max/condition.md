# Algorithms: минимум и максимум

Реализуйте две функции:

- `findMin(arr)` — возвращает минимальное число в массиве
- `findMax(arr)` — возвращает максимальное число в массиве

Без использования `Math.min` / `Math.max`.

```ts
findMin([5, 2, 8, 1, 4]); // 1
findMax([5, 2, 8, 1, 4]); // 8
```
