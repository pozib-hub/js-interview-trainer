## Подсказка 1
Параметры функции имеют приоритет над `var`. `var a` не переназначает параметр `a = 1`.

## Подсказка 2
`b = () => a` — стрелочная функция захватывает `a` из области параметров (= 1). `a = 2` меняет локальную `a`, но не `b`'s замыкание.
