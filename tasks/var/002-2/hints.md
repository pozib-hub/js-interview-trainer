## Подсказка 1
`var i = []` — общий массив для цикла. `i.push(1)` мутирует его. Все `setTimeout` видят финальное значение `[1, 1, 1]`.

## Подсказка 2
Во втором цикле `let val = [...i]` создаёт копию массива на каждой итерации. `setTimeout` видит разные значения.
