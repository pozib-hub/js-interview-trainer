## Подсказка 1
IIFE `((i) => setTimeout(...))(i)` передаёт текущее значение `i` как аргумент. Каждая итерация создаёт свою переменную.

## Подсказка 2
Синхронно: `start`, `end`. Затем 100 `setTimeout` выведут `0, 1, 2, ..., 99` (каждый со своим `i` из замыкания IIFE).
