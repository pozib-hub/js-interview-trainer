# Var: ссылки на объекты

Что выведется в консоль?

```ts
var foo = [1, 2, 3, 4];
var bar = foo;
bar.push(5);

console.log(foo);
console.log(bar);
```

Реализуйте функцию `getResult()`, возвращающую массив строк с выводом консоли (начиная с `"ответ"`).
