## Подсказка 1
`var bar = foo` — копируется ссылка на массив, а не сам массив. `bar.push(5)` мутирует общий массив.

## Подсказка 2
`foo` и `bar` указывают на один и тот же массив. `console.log(foo)` и `console.log(bar)` выведут `[1, 2, 3, 4, 5]`.
