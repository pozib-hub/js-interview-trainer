## Подсказка 1
`var i` — одна переменная для всего цикла. Все стрелочные функции замыкаются на неё. К моменту вызова `i = 100`.

## Подсказка 2
`f[9]()` и `f[8]()` выведут `100`.

## Подсказка 3
Решение: `let i` (блочная область) — каждая итерация создаёт новую переменную в замыкании.
