## Подсказка 1
`pipe(f, g, h)(x)` = `h(g(f(x)))`. Функции применяются слева направо (как compose, но в обратном порядке).

## Подсказка 2
Используйте `reduce` для последовательного применения: `funcs.reduce((acc, fn) => fn(acc), x)`.
