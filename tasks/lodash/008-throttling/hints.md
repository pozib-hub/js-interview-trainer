## Подсказка 1
Throttle гарантирует вызов не чаще одного раза в `ms`. Используйте timestamp последнего вызова.

## Подсказка 2
Разница с debounce: debounce ждёт прекращения, throttle вызывает регулярно. Проверьте leading/trailing edge.
