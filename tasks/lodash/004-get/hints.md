## Подсказка 1
Разделите путь по точкам: `"a.b.c"` → `["a", "b", "c"]`. Итеративно спускайтесь по объекту.

## Подсказка 2
Обрабатывайте массивы: `"a[0].b"` → `obj.a[0].b`. Возвращайте `undefined` если путь не существует.
