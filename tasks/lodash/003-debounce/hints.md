## Подсказка 1
Debounce откладывает вызов до прекращения входных сигналов. Используйте `setTimeout` + `clearTimeout`.

## Подсказка 2
Сохраняйте контекст (`this`) и аргументы через `apply`. Добавьте опцию `immediate` для вызова в начале.
