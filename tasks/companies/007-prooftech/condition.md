# 1) header, main, footer сколько может быть элементов с данным тегом на странице

1) header, main, footer сколько может быть элементов с данным тегом на странице
    2) Что будет если вызвать sort() без коллбэка на числах
    3) Когда и как можно использовать "as" в TS
    4) Рассказать про тип never и примеры
    5) Рассказать на словах как реализовать infiniteScroll ну и в целом про оптимизации рендера списков
    По задачам:
    - написать полифил для метода Array.flat, без второго аргумента на глубину. 
        Написал рекурсивно, потом просили реализовать без рекурсии (через стэк).

    P.S. это именно про собес со стороны Prooftech
