# По порядку:

По порядку:
    1. Что такое HTTP, типы запросов, preflight запрос, как работает CORS и ГДЕ он работает 
    2. Отличие var let const, hoisting, виды областей видимости, как работает замыкание
    3. Операторы ?? ? .?
    4. Для чего нужны библиотеки вроде React, что такое Virtual DOM и как он работает, как работает ререндер в React "под капотом", проп key 
    5. useMemo, useCallback, React.memo (сценарии применения и как работает) 
    6. Redux, flux архитектура, как пользоваться селекторами
    7. Лайвкодинг: проверяли знание useState(value или callback), будет ли перерисовываться компонента при вызове setState с постоянным значением
    8. Реализовать promise.all + какие статические методы Promise знаешь
