# В функцию передается строка и массив положительных чисел, необходимо реализов...

В функцию передается строка и массив положительных чисел, необходимо реализовать функцию так,
  чтобы она возвращала новую строку, в которой будут содержаться те же самые буквы, что и в
  переданной строке, но в другой последовательности, последовательность указана в массиве arr
  в виде индексов букв str.

  restoreString("домик", [4, 1, 0, 3, 2])) => кодим
