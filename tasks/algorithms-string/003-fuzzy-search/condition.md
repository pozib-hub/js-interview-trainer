# Задача — реализовать функцию для проверки,

Задача — реализовать функцию для проверки, 
    содержится ли переданная строка в слове "крокодил" в том же порядке, 
    но не обязательно подряд.

    fuzzySearch("крокодил", "кроко") => true
    fuzzySearch("крокодил", "ид") => false
