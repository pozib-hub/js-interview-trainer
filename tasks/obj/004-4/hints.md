## Подсказка 1
`Count()` возвращает замыкание с приватной переменной `counter`. Присваивание `c.counter = 0` не влияет на замыкание.

## Подсказка 2
Функция — объект, можно добавлять свойства. Но `counter` в замыкании недоступен снаружи. Каждый `Count()` создаёт независимый счётчик.
