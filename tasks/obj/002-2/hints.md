## Подсказка 1
`let b = 4` внутри `c()` — новая переменная в блочной области, не влияет на внешнюю `b`.

## Подсказка 2
`a = 4` — присваивание внешней `var a`. После `c()`: `a = 4`, `b = 3` (не изменилась).
