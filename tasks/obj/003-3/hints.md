## Подсказка 1
Примитивы передаются по значению: `n = 3` внутри `f()` не меняет внешнюю `n`. Объекты — по ссылке.

## Подсказка 2
`f1(obj)` мутирует объект: `obj.a = 5`. `f2(o)` переприсваивает параметр `o = {...}` — не меняет исходный объект.
