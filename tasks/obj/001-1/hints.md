## Подсказка 1
Ключи объекта — всегда строки. При использовании объекта как ключа, он приводится к `"[object Object]"`.

## Подсказка 2
`c[a] = a` и `c[b] = b` — оба используют один ключ `"[object Object]"`. Вторая запись перетирает первую.
