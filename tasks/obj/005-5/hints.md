## Подсказка 1
Первый IIFE мутирует внешний объект `o` (по ссылке) и `i`. Второй IIFE принимает `o, i` как параметры.

## Подсказка 2
Внутри второго IIFE: `o.b = "b"` мутирует объект (по ссылке), но `i++` меняет локальную копию параметра `i`. Внешний `i` не меняется.
